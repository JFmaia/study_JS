/* Operadores condicionais (Ternario)*/

// Dependendo da condição, nós receberemos valores diferentes

// Condição então valor 1 se não valor 2
// condition ? value1 : value2

// Exemplos

// Café da manhã bom
let pao = true;
let carne = true;
const niceBreakfast = pao && carne ? "Perfeito!" : "Errado meu café da manhão!";

// Maior que 18 anos
let age = 18;
const ageApropiety = age >= 18 ? "Maior de idade" : "Menor de idade";


